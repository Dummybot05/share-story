<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Share Story</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        /* Global Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Roboto', sans-serif;
        }

        body {
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
            padding: 20px;
            animation: fadeIn 1.2s ease-in-out;
        }

        /* Header */
        h1 {
            color: #55dd55;
            text-align: center;
            padding: 10px 0;
            animation: slideInFromTop 1s ease-in-out;
            font-size: 3em;
            font-weight: bold;
            letter-spacing: 2px;
        }

        /* Form Styling */
        form {
            background-color: white;
            padding: 40px;
            margin: 0 auto;
            width: 80%;
            max-width: 700px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            animation: fadeIn 1.5s ease-in-out;
        }

        form label {
            font-size: 1.1em;
            margin-bottom: 8px;
            display: block;
            color: #333;
        }

        form input,
        form textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 2px solid #ccc;
            border-radius: 6px;
            font-size: 1em;
            transition: border-color 0.3s;
        }

        textarea {
            height: 240px;
        }

        form input:focus,
        form textarea:focus {
            border-color: #55dd55;
            outline: none;
        }

        form button {
            width: 100%;
            background-color: #55dd55;
            color: white;
            font-size: 1.2em;
            padding: 15px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        form button:hover {
            background-color: #4ebd4e;
        }

        /* Image Upload Styling */
        #img-center {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #img-center input {
            margin-top: 10px;
            font-size: 1em;
        }

        footer {
            text-align: center;
            background-color: #333;
            color: white;
            padding: 20px 0;
            margin-top: 40px;
            font-size: 1em;
        }

        footer b {
            color: #55dd55;
        }

        footer a {
            color: #fff;
            text-decoration: none;
            padding-left: 5px;
        }

        footer a:hover {
            text-decoration: underline;
        }

        /* Keyframe Animations */
        @keyframes fadeIn {
            0% {
                opacity: 0;
            }

            100% {
                opacity: 1;
            }
        }

        @keyframes slideInFromTop {
            0% {
                transform: translateY(-50px);
            }

            100% {
                transform: translateY(0);
            }
        }

        #sub-text {
            text-align: center;
            margin-top: 20px;
            font-size: 1.2em;
            margin-bottom: 20px;
        }
    </style>
</head>

<body>

    <h1>Share Story</h1>
    <h2 id="sub-text">Share your really happened story</h2>

    <form action="/newstory" method="POST">
        <label for="title">Title of a story</label>
        <input name="title" type="text" placeholder="Enter Title" id="title" required />
        <label for="para">Write your story</label>
        <textarea name="para" placeholder="Write your story here" id="para" required></textarea>
        <label for="imgs">Any images (url only)</label>
        <input name="img1" type="text" placeholder="Enter Picture Url 1" id="img1" required />
        <input name="img2" type="text" placeholder="Enter Picture Url 2" id="img2" required />
        <button type="submit">Share Story</button>
    </form>

    <footer>
        <p>
            <b>Share Story</b> &copy; 2023-2024. <a href="#">Privacy Policy</a> | <a href="#">Terms of Service</a>
        </p>
    </footer>

</body>

</html>